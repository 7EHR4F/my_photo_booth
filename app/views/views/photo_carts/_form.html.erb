<%= form_for(@photo_cart) do |f| %>
  <% if @photo_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_cart.errors.count, "error") %> prohibited this photo_cart from being saved:</h2>

      <ul>
      <% @photo_cart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo_id %><br />
    <%= f.text_field :photo_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br />
    <%= f.text_field :cart_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
